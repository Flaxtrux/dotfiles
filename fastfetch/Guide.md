Clone the repo and copy the dotfile its place.
```bash
git clone --depth=1 https://github.com/Flaxtrux/dotfiles.git ~/dotfiles
cp ~/dotfiles/fastfetch/omarchy_lookalike.jsonc .config/fastfetch/config.jsonc
```